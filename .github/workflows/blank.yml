name: CI

on:
  push:
    branches:
      - ci
  pull_request:
    branches:
      - ci

jobs:
  container:
    runs-on: ubuntu-latest
    steps:
#     - name: install biber
#       run: apt-get update && apt-get install -y biber
    - name: build pdf
      run: make docker
